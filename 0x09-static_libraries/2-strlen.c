#include "main.h"
#include <string.h>
/**
* _strlen - returns the length of a string
* @s: returns the length of a string
* Return: length of the string
*/
int _strlen(char *s)
{
	return (strlen(s));
}
